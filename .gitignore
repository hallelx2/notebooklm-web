node_modules
.next
.env
.env*.local
.DS_Store
*.log
dist
build
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
drizzle
.storage


.vercel
.claude

# electron-builder source-resource directory (icons, entitlements, installer
# templates). The default `build` ignore above swallows it; un-ignore so
# `apps/desktop/build/icon.{svg,png}` are tracked.
!apps/desktop/build/
!apps/desktop/build/**
